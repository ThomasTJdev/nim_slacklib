# Package

version       = "0.1.0"
author        = "Thomas T. Jarløv (ThomasTJ)"
description   = "Library for working with a slack app or sending messages to a slack channel"
license       = "MIT"

# Dependencies

requires "nim >= 0.17.0"
